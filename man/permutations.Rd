% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutations.R
\name{permutations}
\alias{permutations}
\alias{print.permutations}
\alias{as.matrix.permutations}
\title{Permute a vector of data}
\usage{
permutations(x, nset, control = how(), check = TRUE, quietly = FALSE)

\method{print}{permutations}(x, ...)

\method{as.matrix}{permutations}(x, ...)
}
\arguments{
\item{x}{A non-empty, one-dimensional atomic vector to permute. Factors are
supported and are returned using their character labels.}

\item{nset}{Numeric; the number of permutations to generate. If missing,
the number is taken from \code{control}.}

\item{control}{An object of class \code{"how"} describing a valid permutation
design.}

\item{check}{Logical; should the permutation design be checked by \code{\link[=check]{check()}}?}

\item{quietly}{Logical; should messages from checking the design be
suppressed?}

\item{...}{Arguments passed to other methods.}
}
\value{
An object of class \code{"permutations"}, inheriting from \code{"matrix"}.
Rows are permutations and columns correspond to the elements of \code{x}. The
object has \code{control} and \code{seed} attributes containing the checked design
and the random-number seed at the start of permutation generation.

\code{as.matrix()} returns an ordinary matrix with the permutation metadata
removed.
}
\description{
\code{permutations()} generates permutations from a specified design and applies
them to the values in a supplied vector. Each row of the returned object is
one permutation of \code{x}.
}
\details{
By default, the permutation design is checked in the same way as for
\code{\link[=shuffleSet]{shuffleSet()}}. Consequently, the number of rows returned can differ from
\code{nset} when the design requests complete enumeration or when there are few
possible permutations. Set \code{check = FALSE} to always request exactly
\code{nset} permutations, which need not be unique.
}
\examples{
\dontshow{suppressWarnings(RNGversion("3.5.0"))}
set.seed(1)
permutations(letters[1:5], nset = 3, check = FALSE)

## Restricted permutations of data from a time series
control <- how(within = Within(type = "series"))
permutations(c(10, 20, 30, 40, 50), nset = 3,
             control = control, check = FALSE)
}
\seealso{
\code{\link[=shuffleSet]{shuffleSet()}} for generating permutation indices and \code{\link[=how]{how()}} for
specifying permutation designs.
}
