% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffle-utils.R
\name{shuffle-utils}
\alias{shuffle-utils}
\alias{shuffleFree}
\alias{shuffleSeries}
\alias{shuffleGrid}
\alias{shuffleStrata}
\title{Utility functions for unrestricted and restricted permutations}
\usage{
shuffleFree(x, size)

shuffleSeries(x, mirror = FALSE, start = NULL, flip = NULL)

shuffleGrid(
  nrow,
  ncol,
  mirror = FALSE,
  start.row = NULL,
  start.col = NULL,
  flip = NULL
)

shuffleStrata(
  strata,
  type,
  mirror = FALSE,
  start = NULL,
  flip = NULL,
  nrow,
  ncol,
  start.row = NULL,
  start.col = NULL
)
}
\arguments{
\item{x}{A vector of indices to permute.}

\item{size}{The number of indices required.}

\item{mirror}{Logical; should mirroring of sequences be allowed?}

\item{start}{Integer; the starting point for time-series permutations. If
missing, a random starting point is determined.}

\item{flip}{Logical of length one for \code{shuffleSeries()} or length two for
\code{shuffleGrid()}; force mirroring of the permutation. For \code{shuffleGrid()},
the first element flips rows and the second flips columns.}

\item{nrow, ncol}{Numeric; the number of rows and columns in the grid.}

\item{start.row, start.col}{Numeric; the starting row and column for the
shifted grid permutation. If not supplied, they are selected randomly.}

\item{strata}{A factor containing the blocks to permute.}

\item{type}{Character; the permutation type used to shuffle \code{strata}. One
of \code{"free"}, \code{"grid"}, \code{"series"}, or \code{"partition"}.}
}
\value{
An integer vector of permuted indices.
}
\description{
Unrestricted and restricted permutations for time series, line transects,
spatial grids, and blocking factors.
}
\details{
These are developer-level functions for generating permuted indices from
restricted and unrestricted designs.

\code{shuffleFree()} is a lightweight wrapper around the code underlying
\code{\link[base:sample]{base::sample()}}. It calls \code{base::sample.int(x, size, replace = FALSE)}
without additional checks. Sampling is without replacement and without
regard to prior probabilities. \code{size} can be one to draw a single index; in
general use it is set equal to \code{length(x)}.

With \code{type = "partition"}, \code{shuffleStrata()} returns one canonical index
permutation for a random arrangement of the labels in \code{strata}; indices
carrying the same original label retain their relative order.
}
\examples{
\dontshow{suppressWarnings(RNGversion("3.5.0"))}
set.seed(3)

## draw 1 value at random from the set 1:10
shuffleFree(1:10, 1)

## permute the series 1:10
x <- 1:10
shuffleSeries(x)                ## with random starting point
shuffleSeries(x, start = 5L)    ## known starting point
shuffleSeries(x, flip = TRUE)   ## random start, forced mirror
shuffleSeries(x, mirror = TRUE) ## random start, possibly mirror

## permute a grid of size 3x3
shuffleGrid(3, 3)                      ## random starting row/col
shuffleGrid(3, 3, start.row = 2,
            start.col = 3)             ## with known row/col
shuffleGrid(3, 3, flip = rep(TRUE, 2)) ## random start, forced mirror
}
\seealso{
\code{\link[=check]{check()}} checks a permutation design, \code{\link[=how]{how()}} describes a design,
and \code{\link[=shuffle]{shuffle()}} is the user-oriented wrapper around these functions.
}
\author{
Gavin Simpson
}
\keyword{design}
\keyword{htest}
